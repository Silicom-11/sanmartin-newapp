<?xml version="1.0" encoding="utf-8"?>
<!--
    Production network policy: all traffic must use HTTPS.
    Cleartext (HTTP) is only allowed for local development hosts, which are
    unreachable in production devices.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Local development backend (adb reverse / emulator loopback) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
